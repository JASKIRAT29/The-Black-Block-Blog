<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Black Block</title>
  <meta name="description" content="Travel Blog" />
  <!-- Core theme CSS (includes Bootstrap)-->
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        Black Block
      </h1>
      <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
        <a href="/dashboard">Dashboard</a>
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    <main>
      <header class="py-5 bg-light border-bottom mb-4 banner">
        <div class="container banner">
          <div class="text-center my-5 ">
            <img src="../../asstes/images/Black.png" alt="logo">
            <h1 class="fw-bolder">Welcome to Travel Guid!</h1>
            <p class="lead mb-0">We Welcomes You - Travel blog Where Adventures Wait</p>
          </div>
        </div>
      </header>

      {{{ body }}}
    </main>
    <footer>
      Copyright (c) 2023 | Team Black Block
    </footer>
  </div>

</body>

</html>

{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}